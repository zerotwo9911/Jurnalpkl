import { redirect } from "next/navigation";
import { getSession } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import AppShell from "@/components/AppShell";
export default async function TempatPkl(){const s=await getSession();if(!s)redirect("/login");if(s.role!=="ADMIN")redirect("/");const rows=await prisma.user.findMany({where:{role:"SISWA",tempat_pkl:{not:null}},select:{tempat_pkl:true},distinct:["tempat_pkl"],orderBy:{tempat_pkl:"asc"}});return <AppShell role="ADMIN" title="Tempat PKL"><div className="page"><div className="detail-head"><div><span className="eyebrow">DATA TEMPAT PKL</span><h1>Tempat PKL</h1><p className="muted">Daftar tempat PKL berdasarkan data siswa yang tersimpan.</p></div></div><div className="student-table">{rows.map((r,i)=><div className="student-row" key={r.tempat_pkl||i}><div><b>{r.tempat_pkl}</b><span>Data dari profil siswa</span></div><span>Aktif</span></div>)}{!rows.length&&<div className="empty-state"><h3>Belum ada tempat PKL</h3><p>Tempat PKL akan muncul setelah data siswa diisi.</p></div>}</div></div></AppShell>}
