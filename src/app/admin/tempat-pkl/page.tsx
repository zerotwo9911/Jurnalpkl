import {redirect} from "next/navigation";import{getSession}from"@/lib/auth";import AppShell from"@/components/AppShell";import PlaceManager from"@/components/admin/PlaceManager";
export default async function Page(){const s=await getSession();if(!s)redirect("/login");if(s.role!=="ADMIN")redirect("/");return <AppShell role="ADMIN" title="Tempat PKL"><div className="page"><div className="detail-head"><div><span className="eyebrow">DATA TEMPAT PKL</span><h1>Tempat PKL</h1><p className="muted">Kelola perusahaan atau instansi tempat siswa menjalani PKL.</p></div></div><PlaceManager/></div></AppShell>}
