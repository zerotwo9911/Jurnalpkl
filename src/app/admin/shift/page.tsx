import {redirect} from "next/navigation";import{getSession}from"@/lib/auth";import AppShell from"@/components/AppShell";import ShiftManager from"@/components/admin/ShiftManager";
export default async function Page(){const s=await getSession();if(!s)redirect("/login");if(s.role!=="ADMIN")redirect("/");return <AppShell role="ADMIN" title="Shift"><div className="page"><div className="detail-head"><div><span className="eyebrow">DATA SHIFT</span><h1>Shift PKL</h1><p className="muted">Atur jadwal kerja yang tersedia untuk jurnal siswa.</p></div></div><ShiftManager/></div></AppShell>}
