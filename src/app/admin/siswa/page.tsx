import {redirect} from "next/navigation";import{getSession}from"@/lib/auth";import AppShell from"@/components/AppShell";import UserManager from"@/components/admin/UserManager";
export default async function Page(){const s=await getSession();if(!s)redirect("/login");if(s.role!=="ADMIN")redirect("/");return <AppShell role="ADMIN" title="Siswa"><div className="page"><div className="detail-head"><div><span className="eyebrow">DATA SISWA</span><h1>Data Siswa</h1><p className="muted">Kelola data siswa, akun, tempat PKL, dan pembimbing.</p></div></div><UserManager mode="SISWA"/></div></AppShell>}
