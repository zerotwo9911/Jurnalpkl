import{redirect}from"next/navigation";import Link from"next/link";import{getSession}from"@/lib/auth";import{prisma}from"@/lib/prisma";import AppShell from"@/components/AppShell";
export default async function Page(){const s=await getSession();if(!s)redirect("/login");if(s.role!=="PEMBIMBING")redirect("/");const students=await prisma.user.findMany({where:{role:"SISWA",pembimbing_id:s.id},orderBy:{nama:"asc"}});const ids=students.map(x=>x.id);const[total,latest]=await Promise.all([prisma.journal.count({where:{user_id:{in:ids}}}),prisma.journal.findMany({where:{user_id:{in:ids}},take:5,orderBy:{created_at:"desc"},include:{user:{select:{nama:true}},shift:true}})]);return <AppShell role="PEMBIMBING" title="Dashboard"><div className="page"><section className="hero"><div><span className="eyebrow">DASHBOARD PEMBIMBING</span><h1>Halo, <span>{s.nama}</span></h1><p className="muted">Ringkasan siswa dan jurnal yang menjadi tanggung jawab Anda.</p></div></section><div className="stat-grid"><Stat n={students.length} label="Siswa Bimbingan"/><Stat n={total} label="Jurnal Masuk"/></div><section className="dashboard-section"><div className="section-heading"><div><span className="eyebrow">SISWA BIMBINGAN</span><h2>Daftar siswa</h2></div><Link className="text-btn" href="/pembimbing/siswa">Lihat semua</Link></div><div className="student-grid">{students.slice(0,6).map(x=><div className="student-card" key={x.id}><b>{x.nama}</b><span>{x.kelas||"-"} · {x.jurusan||"-"}</span><span>{x.tempat_pkl||"Tempat PKL belum ditentukan"}</span><Link className="text-btn" href={`/pembimbing/siswa/${x.id}`}>Lihat jurnal</Link></div>)}{!students.length&&<div className="empty-state compact"><h3>Belum ada siswa</h3><p>Data siswa yang terhubung dengan akun Anda akan tampil di sini.</p></div>}</div></section><section className="dashboard-section"><div className="section-heading"><div><span className="eyebrow">JURNAL TERBARU</span><h2>Jurnal terbaru</h2></div><Link className="text-btn" href="/jurnal">Lihat semua</Link></div><div className="recent-list">{latest.map(j=><Link href={`/jurnal/${j.id}`} className="recent-row" key={j.id}><div><b>{j.activity}</b><span>{j.user.nama} · {j.date.toLocaleDateString("id-ID")}</span></div><span className={`status ${j.status.toLowerCase()}`}>{j.status==="DRAFT"?"Draft":"Terkirim"}</span></Link>)}</div></section></div></AppShell>}
function Stat({n,label}:{n:number;label:string}){return <div className="stat-card glass"><strong>{n}</strong><span>{label}</span></div>}
