import{redirect}from"next/navigation";import Link from"next/link";import{getSession}from"@/lib/auth";import{prisma}from"@/lib/prisma";import AppShell from"@/components/AppShell";
export default async function Page(){const s=await getSession();if(!s)redirect("/login");if(s.role!=="PEMBIMBING")redirect("/");const students=await prisma.user.findMany({where:{role:"SISWA",pembimbing_id:s.id},orderBy:{nama:"asc"},include:{_count:{select:{journals:true}},tempat:true}});return <AppShell role="PEMBIMBING" title="Siswa Bimbingan"><div className="page"><div className="detail-head"><div><span className="eyebrow">DATA SISWA</span><h1>Siswa Bimbingan</h1><p className="muted">Daftar siswa yang terhubung dengan akun pembimbing Anda.</p></div></div><div className="student-table">{students.map(x=><div className="student-row" key={x.id}><div><b>{x.nama}</b><span>{x.kelas||"-"} · {x.jurusan||"-"}</span></div><span>{x.tempat?.nama||x.tempat_pkl||"-"}</span><strong>{x._count.journals} jurnal</strong><Link className="text-btn" href={`/pembimbing/siswa/${x.id}`}>Lihat Jurnal</Link></div>)}{!students.length&&<div className="empty-state"><h3>Belum ada siswa bimbingan</h3><p>Belum ada data siswa yang terhubung dengan akun Anda.</p></div>}</div></div></AppShell>}
