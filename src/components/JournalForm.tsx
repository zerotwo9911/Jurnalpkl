"use client";
import {useEffect,useState} from "react";
type Shift={id:number;name:string;start_time:string;end_time:string};type Initial={id?:number;date:string;shift_id:number;start_time:string;end_time:string;activity:string;description?:string|null;status:"DRAFT"|"TERKIRIM"};
export default function JournalForm({onSaved,onCancel,initial}:{onSaved?:()=>void;onCancel?:()=>void;initial?:Initial}){const[shifts,setShifts]=useState<Shift[]>([]),[shiftId,setShiftId]=useState<number|undefined>(initial?.shift_id),[date,setDate]=useState(initial?.date||new Date().toLocaleDateString("en-CA",{timeZone:"Asia/Jakarta"})),[start,setStart]=useState(initial?.start_time||""),[end,setEnd]=useState(initial?.end_time||""),[activity,setActivity]=useState(initial?.activity||""),[description,setDescription]=useState(initial?.description||""),[saving,setSaving]=useState(false),[msg,setMsg]=useState("");useEffect(()=>{fetch("/api/shifts").then(r=>r.json()).then((data:Shift[])=>{setShifts(data);if(!initial&&data[0]){setShiftId(data[0].id);setStart(data[0].start_time);setEnd(data[0].end_time)}})},[initial]);function changeShift(id:number){const s=shifts.find(x=>x.id===id);setShiftId(id);if(s){setStart(s.start_time);setEnd(s.end_time)}}async function save(status:"DRAFT"|"TERKIRIM"){setMsg("");if(!shiftId||activity.trim().length<5){setMsg("Lengkapi kegiatan dan shift terlebih dahulu.");return}if(initial&&initial.status!=="DRAFT"){setMsg("Jurnal yang sudah terkirim tidak dapat diedit.");return}setSaving(true);const r=await fetch("/api/journals",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:initial?.id,date,shift_id:shiftId,start_time:start,end_time:end,activity,description,status})});const b=await r.json();setSaving(false);if(!r.ok){setMsg(b.error||"Jurnal gagal disimpan.");return}setMsg(status==="DRAFT"?"Draft jurnal berhasil disimpan.":"Jurnal berhasil dikirim.");if(initial) window.location.reload(); else onSaved?.()}return <section className="journal-form-card"><div className="section-title"><div><span className="eyebrow">JURNAL HARIAN</span><h2>{initial?"Edit Jurnal PKL":"Tambah Jurnal PKL"}</h2><p className="muted">{initial?"Perbarui jurnal yang masih berstatus draft.":"Catat kegiatan PKL secara terstruktur."}</p></div><button className="icon-btn" type="button" onClick={onCancel} aria-label="Tutup">×</button></div><div className="form-grid journal-form-grid"><div><label>Tanggal</label><input type="date" value={date} onChange={e=>setDate(e.target.value)}/></div><div><label>Shift</label><select value={shiftId??""} onChange={e=>changeShift(Number(e.target.value))}><option value="" disabled>Pilih shift</option>{shifts.filter(s=>s).map(s=><option key={s.id} value={s.id}>{s.name} · {s.start_time}–{s.end_time}</option>)}</select></div><div><label>Jam mulai</label><input type="time" value={start} onChange={e=>setStart(e.target.value)}/></div><div><label>Jam selesai</label><input type="time" value={end} onChange={e=>setEnd(e.target.value)}/></div><div className="field-span-2"><label>Judul / kegiatan</label><input value={activity} onChange={e=>setActivity(e.target.value)} maxLength={10000}/></div><div className="field-span-2"><label>Deskripsi kegiatan <span className="optional">(opsional)</span></label><textarea value={description} onChange={e=>setDescription(e.target.value)} rows={6} maxLength={10000}/></div></div>{msg&&<div className={msg.includes("berhasil")?"success-box":"error-box"}>{msg}</div>}<div className="action-row"><button className="secondary-btn" type="button" disabled={saving} onClick={()=>save("DRAFT")}>Simpan Draft</button>{(!initial||initial.status==="DRAFT")&&<button className="primary-btn" type="button" disabled={saving} onClick={()=>save("TERKIRIM")}>{saving?"Menyimpan...":initial?"Simpan Perubahan & Kirim":"Simpan Jurnal"}</button>}</div></section>}
